# StageLayout Designer

StageLayout Designer は、イベントや舞台の仕込み図を作るための Java Swing アプリです。

音響、照明、舞台、人物、バミリ、線、テキスト、背景図面をキャンバス上に配置し、提出用プレビュー、PNG保存、PDF保存、印刷まで行えます。
大学サークルや学生団体で、後輩へ引き継ぎやすいことを重視して作っています。

高機能な作図ソフトを置き換えるものではなく、Dynamic Draw などより機能を絞り、初心者でも迷いにくい仕込み図作成ツールを目指しています。

## 現在の版

現在の確認版は `v1.1.1-review` です。

後輩や友人に試してもらうための一時配布版として、GitHub Releases に Zip を添付する想定です。
Zip本体はリポジトリへ直接コミットしません。

配布Zip名:

```text
StageLayoutDesigner-v1.1.1-review.zip
```

Zipには Java runtime を同梱するため、利用者側のPCにJavaを入れてもらわなくても起動できます。

## 画面イメージ

![メイン画面](images/screenshot07091.png)

![プレビュー画面](images/screenshot07092.png)

## 主な機能

### 機材配置

- 左側の機材パレットから機材を選択
- 音響、照明、舞台、人物などのカテゴリ表示
- 音響はスピーカー、マイク・スタンド、その他音響に整理
- 機材画像と表示名の表示
- ドラッグ移動、回転、拡大、縮小、削除
- 選択枠上の回転ハンドルで機材を回転
- よく使う機材をアプリ全体のマイリストとして登録
- 機材やバミリごとの個別ラベル表示/非表示
- 整列ガイドで、ほかの機材の中心線や端に合わせて配置
- 右クリックメニューから名前変更、コピー、前面/背面操作
- 文字、会場パーツ、背景図面も右クリック操作に対応

### バミリ・線

- 通常の線描画
- 赤いバミリ線の描画
- バミリ線はテープ位置として扱い、メートル表示なし
- バミリは横、縦、X、十字、L、T、自由線に対応
- 線の右クリック削除、Delete削除
- 線の始点・終点ドラッグ編集
- 線の太さ変更

### テキストボックス

- 図面上に任意の文字を配置
- 受付、音響卓、出演者待機、立入禁止などの補足説明に利用
- 未選択時は文字だけ表示
- マウスホバー時は薄い枠を表示
- 選択時は枠と右下リサイズハンドルを表示
- 提出用プレビューでは基本的に文字だけ表示

### 背景図面 / PDF読み込み

- PNG / JPG / JPEG / PDF形式のフロアマップを背景図面として読み込み
- PDFは先頭ページを背景図面として使用
- 読み込み時に実寸幅mを入力して背景図面サイズを調整
- 背景図面の表示/非表示、固定/解除、削除、透明度調整
- シート幅合わせ、シート高さ合わせ、実寸幅合わせ、中央配置
- 読み込んだ背景図面を会場テンプレートとして登録可能

### 会場テンプレート

- `舞台 > 会場パーツ` から会場図を作成
- `テンプレート > 図形テンプレートを作成` から、長方形、半円前面、楕円形、陸上トラック風などをサイズ入力で作成
- 214教室、大学野外ステージなどの既存テンプレート
- 会場パーツと背景図面を会場テンプレートとして保存
- 保存した会場テンプレートの読み込み
- `会場固定` による会場パーツの固定

### プレビュー・提出用出力

- 提出用プレビュー表示
- プレビュー範囲の切り替え
  - 作業シート全体
  - ステージ範囲
  - 会場テンプレート
  - 配置物
  - 背景図面
- PNG画像として保存
- PDFとして保存
- 印刷
- イベント名と日付をもとにした保存名
- 必要機材一覧の自動集計
- 必要機材一覧は省略せず、列数や文字サイズで調整して表示
- メモ欄の表示

### 保存・読み込み

- 独自形式 `.stage` で保存
- イベント名、日付、会場名、担当者、メモを保存
- 機材の位置、サイズ、回転、数量、メモ、ラベル表示を保存
- 会場パーツ、線、背景図面、テキストボックスを保存
- 今のファイルを上書き保存
- 会場だけ保存、会場テンプレート読み込みに対応

### アプリ内ヘルプ

- `ヘルプ > 使い方` から基本操作を確認
- 配布後もアプリ内だけで最低限の使い方を確認可能

## 使い方メモ

基本操作や会場テンプレート作成の流れは [使い方メモ.md](使い方メモ.md) にまとめています。

開発者向けのコード説明は [コード補足メモ.md](コード補足メモ.md) にまとめています。

今後の改善候補は [docs/改善点メモ.md](docs/改善点メモ.md) に整理しています。

## 配布について

JDK 21 の `jpackage` を使って、Java が入っていない Windows PC でも動く配布フォルダを作成できます。

現在はインストーラー形式ではなく、フォルダごと配布する形式で確認しています。
配布フォルダには専用の Java runtime が含まれるため、相手のPCに Java を入れてもらう必要はありません。

配布物には以下を含めます。

```text
StageLayoutDesigner/
├ StageLayoutDesigner.exe
├ app/
├ runtime/
├ licenses/
└ README-review.txt
```

GitHub に載せる場合は、配布Zipをリポジトリへ直接コミットせず、GitHub Releases の添付ファイルとして置きます。

## 使用技術

- Java 21
- Swing
- AWT
- Apache PDFBox 3.0.8
- jpackage
- 独自形式 `.stage`

## PDFBoxについて

PDF形式のフロアマップを読み込むために Apache PDFBox を使用しています。

- Library: Apache PDFBox
- Version: 3.0.8
- File: `lib/pdfbox-app-3.0.8.jar`
- License: Apache License 2.0

ライセンス文およびNOTICEは、`licenses` フォルダに収録しています。

EclipseでPDFBoxが認識されない場合は、`lib/pdfbox-app-3.0.8.jar` を右クリックして `Build Path > Add to Build Path` を選択してください。

## 開発・コンパイル

このプロジェクトは Maven / Gradle は使わず、Java 21 の `javac` で直接コンパイルしています。

PowerShell例:

```powershell
javac -encoding UTF-8 -cp ".\lib\pdfbox-app-3.0.8.jar" -d ".\bin" (Get-ChildItem -Path ".\src" -Recurse -Filter "*.java" | ForEach-Object { $_.FullName })
```

画像などの resources を変更した場合は、`src\resources` を `bin\resources` にコピーします。

## 制作意図

大学サークルでは毎年メンバーが入れ替わるため、機材の扱いや図面作成に慣れていない人へ引き継ぐ必要があります。

そのため、このアプリでは機能を増やしすぎるよりも、機材を選んで配置する、必要機材を確認する、保存して次の担当者に渡す、といった基本操作を分かりやすくすることを重視しています。

## 制作背景

大学時代、放送部でイベント運営に関わる中で、ステージ上の機材配置、出演者の立ち位置、スピーカーや照明機材の場所などを分かりやすく共有することの大切さを感じました。

また、前職では照明現場に関わり、機材配置や事前準備の情報共有が現場の動きやすさにつながることを実感しました。

そうした経験から、専門的な図面作成ツールに慣れていない人でも、視覚的に配置を確認できるアプリがあれば便利だと考え、制作を始めました。

## 画像素材について

このアプリでは、一部の機材シンボル画像に Dynamic Draw 関連のアプリ画像・素材を利用、または参考にしたものが含まれています。

そのため、このリポジトリは学習・ポートフォリオ目的の制作物として扱っています。
画像素材の権利は各権利者に帰属します。
再配布、商用利用、公開範囲の拡大を行う場合は、素材の利用条件を確認し、必要に応じて自作素材や利用許可のある素材へ差し替える必要があります。

## 背景図面として読み込むファイルについて

本アプリでは、利用者が所有または利用許可を得た画像やPDFを背景図面として読み込むことができます。

ホールや施設の公式サイトで公開されているフロアマップを利用する場合は、各施設の利用条件を確認してください。
読み込んだ図面データの権利は各権利者に帰属します。
本リポジトリでは、第三者が権利を持つフロアマップ画像やPDFを同梱しません。

## 工夫した点

- 実際のイベント現場で使う機材や情報を想定
- 初心者や後輩に引き継ぎやすい操作性
- 音響、照明、舞台、人物などの現場に近いカテゴリ分け
- 画像付きの機材パレット
- 会場テンプレートをコードではなく操作で作れる方針
- PDF/画像背景を会場テンプレートとして使える構成
- バミリ、線、テキストボックスに対応
- 必要機材一覧の自動集計
- 提出用プレビュー、PDF保存、印刷
- Java が入っていないPCでも動かせる配布形態

## 今後の改善候補

- WiX Toolset を使った Windows インストーラー作成
- スクリーンショットの更新
- 会場テンプレート機能の整理
- CanvasPanel の処理分割
- 必要機材一覧が多い場合の2ページ目出力
- 素材画像の権利確認と差し替え
